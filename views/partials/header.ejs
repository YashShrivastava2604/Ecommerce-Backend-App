<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <nav class="w-full h-[3rem] bg-gray-100 flex items-center justify-between px-4">
        <div class="text-xl">Scatch</div>

        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>
        <% if(loggedin) { %>
            <ul class="flex gap-4">
                <li><a href="/shop" class="text-blue-500 hover:underline">Home</a></li>
                <li><a href="/cart" class="text-blue-500 hover:underline">Cart</a></li>
                <li><a href="/owner/admin" class="text-blue-500 hover:underline">My Account</a></li>
                <li><a href="/user/logout" class="text-red-500 hover:underline">Logout</a></li>
            </ul>
        <% } %>


    </nav>
</body>
</html>